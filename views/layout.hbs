<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>{{title}}</title>
    <link href="/stylesheets/site.css" type="text/css" rel="stylesheet">
    <link rel='shortcut icon' type='image/x-icon' href='/images/favicon.png' />
</head>

<body>
        {{#if loggedInUser}}
            {{>headerAuth}}
        {{else}}
            {{>headerNoAuth}}
        {{/if}}
    <div class="container">

        {{#if errors}}        
        <div id="notifications">
            {{#each errors}}
                <p id="notification-message">{{this}}</p>
            {{/each}}
        </div>
        {{/if}}
    <main>
        {{{body}}}
    </main>

    </div>
    {{>footer}}
</body>

</html>